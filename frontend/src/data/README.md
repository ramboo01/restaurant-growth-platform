# Frontend Mock Data

Frontend screens should initially import small, structured mock datasets from this folder.

Example:

```js
import { mockFoundationItems } from '../../data/foundationData';
```

Later, these imports should be replaced by service/API calls without redesigning the page structure.
